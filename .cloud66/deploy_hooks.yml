production:
  first_thing:
    - source: .cloud66/files/my.cnf
      destination: /etc/mysql/my.cnf
      target: docker
      apply_during: build_only
    - command: sudo mkdir -p /var/lib/mysql
      target: docker
      apply_during: build_only
    
