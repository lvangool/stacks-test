<h1><%= @message %> (<%= ActiveRecord::Base.configurations[Rails.env]['adapter'] %>)</h1>

<h2><%= @balls %></h2>
(<%=Ball.count%> balls created so far)<br/></br>
<img src=<%= asset_path('golf-ball.jpg')%>></img>
New! Newer! Newest! Newester!

And, one more!
<br/>
<br/>
<% new_value = $redis.get('value_now') || 0 %>
VALUE from REDIS is <%= new_value %>
<% $redis.set('value_now', new_value.to_i + 1) %>

